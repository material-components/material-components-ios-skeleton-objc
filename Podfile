# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'ExampleTabbedApp' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # You can import all components to make development easier or
  # individual components to limit dependencies.
  # pod 'MaterialComponents', '~> 38.0'
  pod 'MaterialComponents/AppBar', '~> 38.0'
  pod 'MaterialComponents/Buttons', '~> 38.0'
  pod 'MaterialComponents/FeatureHighlight', '~> 38.0'
  pod 'MaterialComponents/Tabs', '~> 38.0'

  target 'ExampleTabbedAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ExampleTabbedAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
